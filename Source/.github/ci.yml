name: CI
on:
  pull_request:
    branches: [ $default-branch ]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
          lfs: true
      - name: 